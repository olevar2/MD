# Example environment variables for monitoring-alerting-service
# Copy this file to .env and fill in the actual values.

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Service port
PORT=8009

# URLs for the monitoring stack components (adjust if not using default Docker Compose setup)
PROMETHEUS_URL=http://prometheus:9090
ALERTMANAGER_URL=http://alertmanager:9093
GRAFANA_URL=http://grafana:3000

# API key for securing service-to-service communication (replace with a strong key)
API_KEY=replace_with_secure_api_key

# Slack webhook URL for sending alerts (optional)
# SLACK_WEBHOOK_URL=

# SMTP server details for email alerts (optional)
# EMAIL_SERVER=
# EMAIL_PORT=587
# EMAIL_USERNAME=
# EMAIL_PASSWORD=
# EMAIL_USE_TLS=true

# From address for email alerts
EMAIL_FROM=alerts@example.com

# Days to retain metrics data in Prometheus
RETENTION_DAYS=30

global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          # - alertmanager:9093

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Add service scrape configs as they are developed
  # Example for data-pipeline-service:
  # - job_name: "data-pipeline-service"
  #   static_configs:
  #     - targets: ["data-pipeline-service:8000"]
version: '3.8'

services:
  trading-gateway-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8013:8013"
    environment:
      - DEBUG_MODE=true
      - LOG_LEVEL=DEBUG
      - HOST=0.0.0.0
      - PORT=8013
    volumes:
      - ./:/app
    networks:
      - forex-platform-network

networks:
  forex-platform-network:
    driver: bridge
